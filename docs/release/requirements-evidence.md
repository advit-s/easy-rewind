# Easy Rewind Requirement-to-Evidence Matrix

Statuses:

- `not-started` = no implementation work begun.
- `failing` = requirement represented by a reproducible failing check/test.
- `implemented` = code/docs exist but required verification is incomplete.
- `verified` = required executable/manual evidence passes and recovery is recorded.
- `blocked` = cannot proceed due to named unresolved dependency/authority/external action.

| ID | Requirement | Stage | Status | Implementation | Test/command | Evidence | Recovery |
| --- | --- | --- | --- | --- | --- | --- | --- |
| S1-01 | Stop only Easy Rewind backend/Electron processes before backup | Stage 1 | verified | `scripts/legacy/quarantine-legacy.ps1` at `a646b27` | 21-test containment suite plus live classifier pre/post checks | `docs/release/stage-1-verification.md` Safety preflight; confirmed/unresolved/listener/stopped counts all `0` | Relaunch only after purge |
| S1-02 | Copy DB/WAL/SHM/settings together before opening SQLite | Stage 1 | verified | coherent held-handle containment at `a646b27` | 21-test suite; live exact-set and independent size/SHA checks | Protected manifest at `%LOCALAPPDATA%\easy-rewind\legacy-backup\20260724T120608217Z`; report Quarantine evidence | Preserve the only quarantine; use only a disposable copy for recovery/migration work |
| S1-03 | Restrict quarantine to current Windows user | Stage 1 | verified | exact protected ACL implementation at `a646b27` | live independent ACL verification on `6/6` protected objects | `docs/release/stage-1-verification.md` Quarantine evidence | Abort before purge; the verified quarantine remains preserved |
| S1-04 | Record UTC backup time, source paths, sizes, SHA-256 | Stage 1 | verified | protected BOM-free manifest implementation at `a646b27` | live manifest-field checks and `4/4` independent source/backup size/SHA comparisons | Protected `manifest.json`; aggregate-only report evidence | Abort on mismatch; preserve the failed destination/manifest as evidence or discard the invalid destination safely as appropriate, then repeat the complete coherent copy into a new timestamp without mutating or regenerating the old manifest |
| S1-05 | Exclude sensitive/runtime data from Git, builds, exports, logs, tests | Stage 1 | verified | `.gitignore`, `.prettierignore`, and `scripts/hygiene/check-repository.mjs` at `33dd03b` | checker suite `63/63` with `0` skipped; direct Git-mode and filesystem-mode checks passed; release-evidence/root-output ignore assertions passed | `docs/release/stage-1-verification.md` Workspace evidence; spec and quality reviews passed | Restore the reviewed ignore/checker files together and rerun both checker modes |
| S1-06 | Purge DB/WAL/SHM/settings and real `.env` from worktree | Stage 1 | verified | manifest-bound purge removed the coherent four-file data/settings set; Task 5 removed the tracked database and exact original `backend/.env`, leaving only `backend/data/.gitkeep` tracked at `33dd03b` | live purge absence `4/4`, backup rehash `4/4`; exact cleanup pre/post checks; Git-mode and filesystem-mode hygiene checks passed | `docs/release/stage-1-verification.md` Purge and Workspace evidence | Recover data only through a disposable verified quarantine copy; recreate non-secret configuration from `.env.example` and provision a replacement key through the protected channel—never imply that `.env` is in quarantine |
| S1-07 | Remove nested Git repository and generated dependencies | Stage 1 | verified | exact cleanup removed `backend/.git`, `backend/node_modules`, and `tmp_test.js`; hygiene enforcement at `33dd03b` | all three exact targets existed before and were absent after; root `.git` and quarantine remained present; checker suite `63/63` and both direct modes passed | `docs/release/stage-1-verification.md` Purge and Workspace evidence; spec and quality reviews passed | Reinstall dependencies with the normalized workspace's `npm ci` after Task 6; restore source only from the root Git repository |
| S1-08 | Add placeholder-only `.env.example` | Stage 1 | not-started | `backend/.env.example` | Secretlint | Stage 1 report | Recreate from tracked example |
| S1-09 | Select Node LTS, engines, exact dependency pins, one lockfile | Stage 1 | not-started | workspace manifests | `npm ci` and version checks | Stage 1 report | Revert manifests/lock together |
| S1-10 | Root scripts install, develop, lint, format, unit/integration test, validate extension, build, package Windows, verify, and scan | Stage 1 | not-started | root `package.json` | `npm run verify` | Stage 1 report | Run component commands directly |
| S1-11 | Add CI secret scanning and repository hygiene | Stage 1 | not-started | `.github/workflows/ci.yml` | workflow lint/readback | CI run URL | Revert CI separately |
| S1-12 | Document Git-history purge separately | Stage 1 | not-started | history guide | command review | Stage 1 report | Mirror backup before rewrite |
| S1-13 | Require Gemini key revocation and replacement | Stage 1 | not-started | credential response guide | manual confirmation | private incident-record reference | Revoke again if uncertain |
| S1-14 | Never migrate or import during Stage 1 | Stage 1 | verified | Stage 1 containment boundary and `sqliteOpened=false` manifest | 21-test containment suite; live manifest assertion; no SQLite/API invocation | `docs/release/stage-1-verification.md` Safety preflight and Quarantine evidence | Stop and discard any disposable working copy; preserve the quarantine |
